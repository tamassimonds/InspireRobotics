/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Enables React Strict Mode
    swcMinify: true,       // Uses SWC to minify the JavaScript
    // You can add more configuration options here
  };
  
  module.exports = nextConfig;