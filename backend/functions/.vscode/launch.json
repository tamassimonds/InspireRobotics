{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: main.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py", // Points to main.py in your workspace folder
            "console": "integratedTerminal",
            "pythonPath": "${command:python.interpreterPath}"
        }
    ]
}